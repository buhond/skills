interface:
  display_name: 'Code Review'
  short_description: 'Review a diff with one agent per rule, verify every blocker, then fix the code'
  default_prompt: 'Use $code-review to review this diff and fix what it finds.'
