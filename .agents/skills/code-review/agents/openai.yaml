interface:
  display_name: 'Code Review'
  short_description: 'Review a diff with one agent per rule, then verify every blocking finding'
  default_prompt: 'Use $code-review to review this diff with one agent per rule and a pass/fail verdict.'
