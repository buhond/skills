interface:
  display_name: "Skill Auto Update"
  short_description: "Turn feedback into durable skill updates"
  default_prompt: "Use $skill-auto-update to distill reusable lessons from feedback, update the right skill, avoid duplicates, and strengthen repeated rules."
